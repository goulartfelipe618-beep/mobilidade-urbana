# Transporte.PRO — Driver Web (PWA)

Painel web do motorista: ficar online, receber ofertas, operar corrida e ver ganhos.

## Desenvolvimento

```bash
npm install
npm run dev
```

API padrão: `http://localhost:3000` (variável `VITE_API_BASE`).

## Build

```bash
npm run build
npm run preview
```
